# ตำหนักผู่โถวเจ้าแม่กวนอิม · 普陀觀音堂

เว็บไซต์ตำหนักผู่โถวเจ้าแม่กวนอิม ถนนพุทธมณฑลสาย ๒
เป้าหมายของเว็บนี้คือเป็นแหล่งข้อมูลที่ถูกต้องและครบที่สุดเรื่อง
“เจ้าแม่กวนอิม ๓๓ ปาง” ในภาษาไทย โดยผูกความรู้นั้นเข้ากับองค์จริงที่ประดิษฐานอยู่ในตำหนัก

**หมุดหมายที่ทุกอย่างล้ออยู่: พิธีเปิดตำหนัก ๒๕ ตุลาคม ๒๕๖๙**

---

## เริ่มใช้งาน

```bash
npm install
npm run dev          # เปิด http://localhost:4321
npm run build        # สร้างเว็บลง dist/
```

ต้องใช้ Node 22 ขึ้นไป

## คำสั่งทั้งหมด

| คำสั่ง | ทำอะไร |
|---|---|
| `npm run dev` | เซิร์ฟเวอร์สำหรับพัฒนา |
| `npm run build` | สร้างเว็บ HTML คงที่ลง `dist/` |
| `npm run preview` | ดูผลลัพธ์ที่สร้างแล้ว |
| `npm run images` | `photos-src/` → WebP ๓ ขนาด ลง `public/images/` |
| `npm run og` | สร้างภาพพรีวิวตอนแชร์ ๓๔ ไฟล์ ลง `public/og/` |
| `npm run qr` | แผ่นพิมพ์ป้าย QR ๓๕ ชิ้น ลง `dist-qr/qr-sheet.html` |
| `npm run lunar -- 2027` | ร่างวันสำคัญจันทรคติของปีนั้น |
| `npm run import:sheet -- <url>` | Google Sheet → ไฟล์ข้อมูล ๓๓ ปาง |

## โครงสร้าง

```
src/content/pang/       ๓๓ ไฟล์ — หนึ่งปางหนึ่งไฟล์
src/content/wishes/     ๘ หมวดพร (เป็นทั้งตัวกรองและหน้าเว็บ)
src/content/events/     ปฏิทินวันสำคัญ แยกไฟล์ตามปี พ.ศ.
src/content/news/       ข่าวและประกาศ (เจ้าหน้าที่ลงเองผ่าน /admin)
src/content/prayers/    บทสวด
src/data/site.json      ที่อยู่ เวลาเปิดปิด ช่องทางติดต่อ วันพิธีเปิด
src/pages/              หน้าเว็บตามผัง
scripts/                สคริปต์ช่วยงาน (ภาพ · QR · ปฏิทิน · นำเข้าข้อมูล)
photos-src/             ภาพต้นฉบับจากช่างภาพ (ไม่เข้า git)
public/admin/           หน้าจอจัดการเนื้อหา Decap CMS
```

## หลักการที่ยึดไว้

**ยึดชื่อจีนเป็นหลัก** ชื่อไทยของแต่ละปางในไทยมีหลายสำนวน ระบบจึงเก็บทั้ง
ชื่อทั่วไป (`name_th`) และชื่อที่ป้ายในตำหนักใช้จริง (`name_th_shrine`)
ถ้ามีชื่อของตำหนัก เว็บจะแสดงชื่อนั้น และเอาชื่ออื่นไปไว้ท้ายหน้าเป็น “ชื่ออื่นที่พบ”
เพื่อให้คนที่ค้นด้วยชื่อไหนก็เจอ

**เนื้อหาที่ยังไม่ได้ตรวจ จะไม่เผยแพร่** ปางที่ฟิลด์ `verified_by` ยังว่าง
จะยังมีหน้าอยู่ (ป้าย QR ที่พิมพ์ไปแล้วต้องใช้ได้) แต่แสดงเฉพาะชื่อ ภาพ
ลักษณะเด่น และหมวดพร พร้อมป้ายแจ้ง และไม่ส่งให้ Google เก็บดัชนี
เพราะเว็บนี้จะกลายเป็นแหล่งอ้างอิงของคนอื่นต่อไป

**องค์ที่ยังไม่มา ไม่ซ่อน** ปางที่ `enshrined: false` ยังอยู่ในตาราง
แสดงเป็น “กำลังอัญเชิญ” โดยเลขลำดับ ๑–๓๓ ยังครบ

**QR ชี้ที่ `slug` ไม่ใช่เลขลำดับ** ถ้าตำหนักเปลี่ยนลำดับปางภายหลัง
ป้ายที่พิมพ์ไปแล้วยังใช้ได้ — แต่ **ห้ามเปลี่ยน `slug` หลังส่งพิมพ์ป้ายแล้ว**

**หน้าแรกสลับเองตามวัน** ก่อน ๒๕ ต.ค. ฮีโร่หน้าแรกคือพิธีเปิด
หลังวันงานสลับกลับเป็นหน้าปกติ ทำได้เพราะ CI สร้างเว็บใหม่ทุกวัน
ไม่ต้องมีใครไปกดอะไร

**ใช้งานได้โดยไม่มี JavaScript** ตัวกรองพรบนหน้ารวมเป็นลิงก์ไป `/wish/{หมวด}`
เมื่อไม่มี JS และกรองในหน้าเมื่อมี JS · แถบเวลาเปิดปิดแสดงทั้งสัปดาห์
เมื่อไม่มี JS และเปลี่ยนเป็น “วันนี้เปิด…” เมื่อมี

## งานที่ยังค้าง

- [ ] ภาพองค์ ๓๓ ปาง — ดูสเปกใน [`photos-src/README.md`](photos-src/README.md)
- [ ] ชื่อไทยและลำดับปางที่ป้ายในตำหนักใช้จริง
- [ ] องค์ที่ยังขาด ๕ องค์คือปางไหน (ตอนนี้ตั้ง `enshrined: true` ไว้ทั้งหมด)
- [ ] คำอธิบายปางละ ๑๕๐–๓๐๐ คำ และใส่ชื่อผู้ตรวจใน `verified_by`
- [ ] ประวัติตำหนัก ๔๐๐–๘๐๐ คำ
- [ ] ขั้นตอนการไหว้ตามที่ตำหนักปฏิบัติจริง (ตอนนี้เป็นฉบับตั้งต้น)
- [ ] บทสวดฉบับที่ตำหนักใช้ พร้อมคำอ่านไทย
- [ ] กำหนดการพิธีเปิด ๒๕ ต.ค.
- [ ] เบอร์โทร ไลน์ OA และเพจเฟซบุ๊ก ใน `src/data/site.json`
- [ ] เทียบวันที่มีเครื่องหมาย `≈` กับปฏิทินจีน (通勝) แล้วตั้ง `approx: false`
- [ ] โดเมนจริง (ตอนนี้ตั้งเป็น `putuoguanyin.com` ใน `site.json`)
- [ ] Google Business Profile — **ยืนยันตัวตนใช้เวลา ๑–๒ สัปดาห์ ต้องเริ่มก่อน**

## เอกสารเพิ่มเติม

- [`docs/CONTENT.md`](docs/CONTENT.md) — วิธีเพิ่ม/แก้เนื้อหา
- [`docs/CMS.md`](docs/CMS.md) — ตั้งค่าหน้าจอ `/admin` ให้ตำหนักใช้เอง
- [`docs/DEPLOY.md`](docs/DEPLOY.md) — ขึ้นเว็บจริง
